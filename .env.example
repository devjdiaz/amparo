# --- Supabase -------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# --- Modelos --------------------------------------------------------------
ANTHROPIC_API_KEY=
OPENAI_API_KEY=            # Whisper (transcripción) + embeddings del corpus

# --- Operación ------------------------------------------------------------
# En true nada del camino del demo toca la red. Es el modo por defecto:
# para apagarlo hay que decirlo explícitamente.
MODO_FIXTURE=true
# Congela la fecha de corte para que la inmediatez no derive entre la
# grabación del video y la entrega. Vacío = hoy.
FECHA_CORTE=
