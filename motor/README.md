# Motor de procedibilidad

TypeScript puro. Sin framework, sin red, sin base de datos.

```
pnpm vitest run
```

Documentación completa, capa por capa, en el vault de Obsidian: `03 Técnico/Motor de procedibilidad.md`.

| Archivo | Capa |
|---|---|
| `tipos.ts` | Memoria de hechos — un hecho sin origen no entra |
| `compuertas.ts` | Las cuatro reglas de procedibilidad (conjuntivas) |
| `fuerza.ts` | Scorecard de pesos (compensable, nunca niega) |
| `recuperador.ts` | Recuperación de jurisprudencia (determinista, cero es válido) |
| `validador.ts` | Aduana — verifica que el redactor no afirme sin respaldo |
| `certificado.ts` | Certificado de auditoría (se emite siempre) |
| `casos.test.ts` | Suite de casos de referencia |
